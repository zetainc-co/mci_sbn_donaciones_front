# AI Framework - Project .gitignore
# Auto-installed by AI Framework plugin
# Prevents framework files from being committed to your project repository

# ============================================================================
# FRAMEWORK FILES (installed but not committed)
# ============================================================================

# Framework internals (rules, configuration)
/.claude/
/.specify/

# Framework configuration files (ignored by default)
/CLAUDE.md

# Local configuration overrides (user customizations, not shared with team)
# Uncomment to version these files (not recommended):
# settings.local.json
# config.local.json
# CLAUDE.local.md

# MCP server data directories
/.playwright-mcp/

# ============================================================================
# FRAMEWORK RUNTIME (logs, databases, temp files)
# ============================================================================

# Hook databases and cache (if user creates custom hooks)
/hooks/*.db
/hooks/__pycache__/

# ============================================================================
# USER ARTIFACTS (version control is user's choice)
# ============================================================================

# Specs and PRPs are user-generated artifacts created by framework commands.
# By default, they are NOT ignored (can be versioned for documentation).
# To ignore them, uncomment these lines:
# /specs/
# /prps/

# ============================================================================
# COMMON PROJECT FILES (add your own)
# ============================================================================

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Editor directories
.vscode/
.idea/
.cursor-settings.json
*.swp
*.swo
*~

# Dependencies
node_modules/
vendor/
jspm_packages/
.npm

# Python
__pycache__/
*.pyc
*.py[cod]
*$py.class
*.so
.pytest_cache/

# Test coverage
.coverage
coverage/
*.lcov
.nyc_output

# TypeScript
*.tsbuildinfo

# Build outputs
dist/
build/
.next/
.expo/
.parcel-cache

# Logs
*.log

# Backup and temporary files
*.backup
*.bak
*-backup.*
*-fixed.*
*-temp.*
*.orig
backup/
backups/
tmp/
temp/
*.tmp

# Playwright MCP test artifacts (if using Playwright MCP server)
test-results/
playwright-report/
blob-report/
.local-browsers/
drivers/
.auth/
*.auth.json
