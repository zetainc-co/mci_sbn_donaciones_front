
  # Multi-Step Donation System Prototype

  This is a code bundle for Multi-Step Donation System Prototype. The original project is available at https://www.figma.com/design/if0x053bfEkRHeZ0ny09Tz/Multi-Step-Donation-System-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  